{
  "name": "playlist-functions",
  "description": "Cloud Functions for the playlist project",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "firebase deploy --only functions",
    "build:watch": "tsc -w"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google-cloud/tasks": "^6.2.1",
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}